import React, { Conponent } from "react";

class AppClass extends Conponent {
    render() {
        const name = 'react';
        return <div className="react">{name}</div>
    }
}

export default AppClass;